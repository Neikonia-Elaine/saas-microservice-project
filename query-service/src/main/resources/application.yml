server:
  port: 8086

spring:
  application:
    name: query-service

  datasource:
    url: jdbc:postgresql://localhost:5432/microserver_db
    username: fanzhiyuan
    password:

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
